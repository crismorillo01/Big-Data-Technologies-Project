# =============================================================================
# Vulnerability Intelligence Platform — pinned dependencies
# Tested on Python 3.10 / 3.11 (macOS arm64 and Linux x86_64).
# =============================================================================

# ---- Core compute ----------------------------------------------------------
pyspark==3.5.1
pandas==2.2.2
numpy==1.26.4
pyarrow==15.0.2
scikit-learn==1.5.0          # Davies-Bouldin index for clustering K selection

# ---- HTTP ingestion --------------------------------------------------------
requests==2.32.3
tenacity==8.5.0              # download retries with exponential backoff

# ---- Serving layer ---------------------------------------------------------
duckdb==1.0.0                # SQL over Parquet, no server

# ---- Streamlit app ---------------------------------------------------------
streamlit==1.36.0
plotly==5.22.0               # used for cluster scatter and simulation timelines

# ---- Testing ---------------------------------------------------------------
pytest==8.2.2

# ---- Orchestration (OPTIONAL) ---------------------------------------------
# Only install if Session 8 (Airflow) is in scope. Comment out otherwise to
# avoid pulling Airflow's heavy dependency tree.
# apache-airflow==2.9.2
